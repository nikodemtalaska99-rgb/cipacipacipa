diploiVersion: "v1.0"
project: discord-bot

components:
  - identifier: bot
    package: nodejs-20
    scripts:
      run: node index.js
    env:
      TOKEN: ${TOKEN}
    persistence:
      - path: /app/assets
        size: 1Gi